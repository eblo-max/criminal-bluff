/**
 * Файл инициализации Sentry для мониторинга ошибок
 * Этот файл должен быть импортирован в начале entry point приложения
 */

// Импортируем и реэкспортируем Sentry из конфигурационного файла
const { Sentry } = require('./config/sentry');

// Экспортируем Sentry для использования в других частях приложения
module.exports = Sentry; 